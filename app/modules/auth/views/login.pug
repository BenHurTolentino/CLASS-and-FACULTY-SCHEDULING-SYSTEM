extends ../../../templates/base

block body
    - var hasIncorrect = typeof incorrect !== 'undefined' ? 'Incorrect username or password.' : ''
    - var hasUnauthorized = typeof unauthorized !== 'undefined' ? 'You are not authorized to view the page you\'re trying to access. Please log in first.' : ''
    div.ui.container.height5
        div.ui.middle.segment.grady
            div.ui.container
                label.ui.inverted.black.label.t-align.white.fontJockey.fluid.huge Class and Faculty Scheduling System        
        div.ui.horizontal.segments
            div.ui.segment
                form.ui.form(method="POST" action="/login")
                    div.sixteen.wide.field.iverted
                        label.white Username
                        input(type="text" name="username" placeholder="Username")
                    div.sixteen.wide.field
                        label.ui.teal Password
                        input(type="password" name="password")
                    div.height3
                        button#but.ui.basic.button.sixteen.wide.black.field.height7(type="submit" value="Login")
                            i.sign.in.icon 
                            p Login 
            div.ui.right.aligned
                    img(src="/assets/files/logo.png" height="250px" width="350px")
        script
            